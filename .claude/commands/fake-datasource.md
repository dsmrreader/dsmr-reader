Start the fake datasource to generate test data with gas and electricity returned:

```bash
poetry run /app/src/manage.py dsmr_fake_datasource --with-gas --with-electricity-returned
```

Report the result.

